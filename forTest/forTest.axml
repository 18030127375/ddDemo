<view>
  <form onSubmit="onSubmit">
    <view a:for="{{list}}" key="{{item.num}}">
      {{index}}.<input name="test{{index}}" value="{{item.text}}" /> <text onTap="del" data-index="{{index}}">删除</text>
    </view>
    <button type="primary" form-type="submit">保存</button>
  </form>
</view>